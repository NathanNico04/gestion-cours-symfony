version: '3'
services:
  php-fpm:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        GOODUSER: zarkos
        UID: 1000
        MAIL: nathan.nicolessi@univ-orleans.fr
        NOM: "Nathan Nicolessi"
    container_name: my-good-container
    volumes:
    - ./dev:/var/www/html
    ports:
    - "5000:8000"
    environment:
      PHP_IDE_CONFIG: serverName=toto
